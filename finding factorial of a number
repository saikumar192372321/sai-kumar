#include <stdio.h>

int main() {
    int number = 5; 
    int factorial = 1;

    for (int i = 1; i <= number; i++) {
        factorial *= i;
    }

    printf("Factorial of %d = %d", number, factorial);
    return 0;
}
OUTPUT:
Factorial of 5 = 120
